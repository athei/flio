- replace nom* with explicit macros
- add repr to all c-like enums
- convert ntstatus to camelCase
- add function to nt status to get severity
    - is_success
    - component part
- fail parse when unknown status instead as of using an unknown error type

- remove derive debug
- rename smb2 -> smb2-packet